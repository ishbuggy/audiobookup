# --- Python ---
# Byte-compiled / optimized / C extensions
__pycache__/
*.py[cod]
*$py.class

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual Environments
.venv/
venv/
ENV/
env/
# NOTE: Your local .venv is now ignored.

# --- Node.js ---
# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# Dependency directories
node_modules/
jspm_packages/

# --- Editor & Tooling ---
# VSCode
.vscode/

# Ruff Linter Cache
.ruff_cache/

# --- Project Specific ---
# User-generated data and configuration.
# These directories are created by the user on their host and should NOT be in the repo.
appdata/
/appdata/config/
/appdata/database/
audiobooks/

# Local configuration files that might contain secrets or user-specific paths
# (settings.json should be generated by the app, not tracked in source control)
src/audible_downloader/settings.json
/config/settings.json

# Docker override files should NOT be committed to the repository.
docker-compose.override.yml

# --- OS-generated files ---
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db